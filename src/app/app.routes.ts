import { Routes } from '@angular/router';
import { StorageUtilsComponent } from './storage-utils/storage-utils.component';

export const routes: Routes = [{ path: 'storage-utils', component: StorageUtilsComponent }];
